import React, { useState } from "react"; // Importe React et useState depuis le module "react"

const Profile = () => {
  return <div>Profil</div>;
};

export default Profile; // Exporte le composant
